import copy
import re
from collections import deque

def check(i, d, s):
    for k in range(len(d)):
        for l in range(k + 1, len(d)):
            if d.index(k) + d.index(l) == i:
                return True
    return False

d = deque()
s = set()

with open("input") as f:
    for l in f:
        try:
            i = int(str(l[0:-1]))
            if i in d:
                #print(i)
                raise Exception("Boof")
                
            if len(d) == 25:
                #print(repr(d))
                if not check(i, d, s):
                    print("# " + str(i))
                    raise Exception("Blah")
                s.remove(d.popleft())
            d.append(i)
            s.add(i)
        except Exception:
            continue
